#we will clone an python repo
477  cd /root
  478  git clone https://github.com/cloudarchgopal/travisproject.git
  479  cd travisproject/
  480  ls
  481  rm -rf .git
  
  #Open your personal git
  create a repo called as travisproject
  once you create the repo intialized it
  #create a token in github 
  
  #we need to login to travis ci. 
  and import the project
